# T4.13 — Phase 4 (Owner) exit record

**Status: PHASE EXIT NOT DECLARED.** One criterion is measurable from the repo and passes
(§3, card-vs-row). The other six are the owner's two-week parallel run and are **PENDING** —
they cannot be produced from code. This file records what is measured, what is pending, and
how to finish each pending box when the run completes.

**Measured:** 2026-09-15, `main` @ `877d10a` (stages 1–4 merged; T4.9 `9588339`,
T4.11 `877d10a`). **Run window:** not started as of this date — the phase's screens finished
merging the same morning, so no field data exists yet.

---

## Exit criteria — `PLAN-EXECUTION.md` Phase 4 exit

### 1. Full week of cash reconciliation entirely in-app, ≥1 real variance or missing submission caught — **PENDING**

The owner runs one full week of reconciliation in the cash queue while keeping his own paper
figures; the two are compared at week's end. Record here: days covered, rows confirmed /
disputed / reopened, and **every variance or `missing_submission` the queue flagged that the
paper figures missed** (or the reverse). If the week produces **zero** flags, do not tick this
box — investigate first (the business is unusually tidy, the queue range is wrong, or the view
is wrong). The queue's default range ends yesterday; today is reachable and captioned
"Today — still syncing. Figures settle overnight." Fallback if the API is down:
`docs/implementation/T4.9-RUNBOOK.md` (psql over `v_cash_reconciliation_queue`).

Raw numbers: _pending run_

### 2. *Locate now* ≤ 60 s on ≥ 80% of attempts; failures show "device has not answered" — **PENDING**

Needs the roster handsets in the field. Record here: attempts, successes within 60 s, and
confirmation that every failure rendered "device has not answered" rather than spinning.
Carried items that bear on this: the unanswered-path and pulse-on-blur handset checks left
open by T4.10, and the **map tile source owner decision** (`EXPO_PUBLIC_MAP_STYLE_URL`
still unset — the console runs as an honest no-tiles roster pane until decided). Note the
roster health signal does not depend on tiles.

Raw numbers: _pending run_

### 3. No owner screen renders a card grid on desktop — **PASS (code audit + tests, 2026-09-15)**

Method: structural audit of every owner screen plus the width tests.

- **List screens (10)** — `cash-queue`, `JobsScreen`, `CustomersScreen`, `SalesScreen`,
  `PaymentsScreen`, `CompaniesScreen`, `ContractsScreen`, `EmployeesScreen`,
  `ProductsScreen`, `ServicesScreen` — all consume `useDensity()` (fed by NavShell's
  `DESK_MIN_WIDTH = 1024`) and render `desk ? <DeskTable> : <phone cards>`: sortable
  columns, scannable left edge, one row per entity.
- **Console screens (2)** — `dashboard.tsx` renders the desk stats as a **4-up stat row**
  with the charts side by side (source comment: "figures on a desk are a row, not a card
  grid"); `location.tsx` renders three panes with a sortable roster row list
  (source comment cites the screen rule directly).
- **Detail/form screens (5)** — `CompanyDetail`, `ContractDetail`, `EmployeeDetail`,
  `EmployeeForm`, `OwnerProfile` — single-entity layouts, no grids.
- **Tests:** `jobs.test.tsx` and `customers.test.tsx` assert card-vs-row at
  390 / 800 / 1024 / 1440 and pin `DESK_MIN_WIDTH = 1024` by source-read; the remaining
  list screens inherit the rule structurally (same switch, same primitive).

Caveat carried: T4.7's live-browser 500-row/60fps re-measure (and a full per-screen
screenshot pass at 360dp / 412dp / 1280px / 1920px) remains an open item — this box is
ticked on code + unit evidence, not on live-browser screenshots.

### 4. Owner completed one employee admin task (create / deactivate / password reset) unaided — **PENDING**

Record which task, and that it was done without developer help. The API side is tested:
deactivation with open jobs → 409 listing them; refused while an unconfirmed/disputed
reconciliation is held; tokens revoked and the employee leaves the health view on success.

Raw numbers: _pending run_

### 5. Web and Android show the same figures for the same day — checked by hand once, deliberately — **PENDING**

Pick one day and one figure (e.g. an employee's expected cash for a closed day), open the
same screen in the web bundle and on the APK, and record both numbers here. Both apps read
the same API and the same IST day boundaries; the check exists to catch a web-only caching
or timezone bug.

Raw numbers: _pending run_

### 6. Owner amended ≥ 1 completion, reason recorded, queue reflected it — **PENDING**

The machinery is integration-tested (amend gated on `owner.amend`; amend after confirm →
409 with reopen offered, naming the reconciliation; reopen requires its own reason and
never auto-amends; the queue reflects the new figure). What is pending is the owner having
driven it once for real. Record: job id, old/new amount, reason given, and that the queue
row moved.

Raw numbers: _pending run_

### 7. Dashboard: four defined stats and two charts, and the owner can say what each means unprompted — **PENDING (design test)**

Ask him, without prompting, what each stat and chart means as he uses the desk during the
run. If he cannot say, **the labels are wrong** — fix labels, not the owner. Record his
words per stat/chart.

Raw numbers: _pending run_

---

## Local gate (measured on `main` @ `877d10a`, 2026-09-15)

| Command | Result | Exit |
|---|---|---|
| `pnpm test` | api: 52 files, 778 passed \| 2 skipped (780) · mobile: 60 files, 529 passed (529) · shared: pass | 0 |
| `pnpm -F api test:int` | 42 files, 573 passed \| 2 skipped (575) | 0 |
| `pnpm -F api test:authz` | 7 files, 176 passed (176) — matrix shrink guard incl. `GET /v1/cash/queue/day` and `GET /v1/jobs/:id/events` | 0 |
| `pnpm -F mobile test` | 60 files, 529 passed (529) | 0 |
| `pnpm e2e` | **not runnable on athena** — no Maestro, no adb, no attached device (environmental, known since T4.10) | n/a |

No mocked database anywhere; all suites ran against the compose Postgres.

## Rollback tiers (verified in `packages/shared/src/flags.ts`)

`owner.web` (desktop rail + DataTable) · `owner.location` (console + map) · `owner.cash`
(reconciliation queue) · `owner.amend` (completion amendment). Every flag defaults **off**
(`defaultFeatureFlags()` → all false), evaluated per employee at `GET /v1/auth/me`. Web is a
separate bundle and rolls back independently of the APK (T1). The cash queue is not
descopable — it is the reason the phase exists.

**Descope order if the run surfaces problems:** (1) dashboards reduce to counts; (2) location
console reduces to roster + last-seen, no map; (3) employee admin moves to direct SQL for a
few weeks.

## Finishing this record

After the two-week run: fill each PENDING "Raw numbers" line, tick the corresponding box in
`PLAN-EXECUTION.md` Phase 4 exit only on real evidence, and merge this branch. If a box
cannot be ticked honestly, the phase is not done — the rollback table above names the tier
and the flag for each failure shape.
