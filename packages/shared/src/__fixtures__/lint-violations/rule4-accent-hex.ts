/**
 * Fixture — must fire: accent-erosion defence. The safety-yellow accent
 * lives only in a theme.ts; everywhere else it is imported.
 */

/** Deliberate violation: a hand-typed accent hex in a non-theme module. */
export const dangerColor = '#F2C200';
